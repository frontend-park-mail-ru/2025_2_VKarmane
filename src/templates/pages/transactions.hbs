<div class="container">
        <div class="LeftPart">
            {{{menu}}}
        </div>
        <div id="main">
            <div class="main__header">
                <h1 class="general">Управление транзакциями</h1>
                {{{profile_block}}}
            </div>
            <div class="Transact-categories">
                {{{ transactions }}}
                {{{ categories }}}
            </div>
            {{{ addOperations }}}
            {{{ addCategories }}}
            {{{ redactOperations }}}
            {{{ redactCategories }}}
        </div>
</div>
